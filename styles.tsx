import { NativeBaseProvider, Box, Heading, Button, VStack, Progress, } from "native-base";
import { ImageBackground, View, Text, StyleSheet, Image } from "react-native";

const styles = StyleSheet.create({


    backgroundImage: {
        justifyContent: "center",
        alignItems: "center",
        flex: 1,
        resizeMode: "contain",
      },
      background2: {
        backgroundColor: "white",
        flex: 1,
        alignItems: "center",
      },

});

export default styles;